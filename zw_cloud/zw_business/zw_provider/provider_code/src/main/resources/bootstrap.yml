spring:
  application:
    name: @project.name@
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: PROVIDER_GROUP
